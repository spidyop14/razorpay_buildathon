import test from "node:test"; import assert from "node:assert/strict"; import { getProduct } from "../catalog.ts"; import { generateGrowthOpportunities } from "./growth-agent.ts"; import { buildGrowthOffer } from "./offer-engine.ts";
const laptop=getProduct("lap-001");
test("detects and ranks related cross-sells",()=>{const r=generateGrowthOpportunities(laptop,{raw:"coding laptop",category:"laptop",maxBudget:60000,requirements:{},useCase:"coding"});assert.ok(r.length>1);assert.ok(r[0].score>=r.at(-1).score);assert.ok(r[0].relatedProducts.every(x=>laptop.relatedProductIds.includes(x.id)))});
test("calculates the deterministic bundle financials",()=>{const offer=buildGrowthOffer(laptop,[getProduct("mouse-001"),getProduct("bag-001")]);assert.equal(offer.subtotal,57997);assert.equal(offer.discountAmount,597);assert.equal(offer.proposedPrice,57400);assert.ok(offer.merchantMargin>=3000)});
test("respects remaining buyer budget and stock",()=>{const r=generateGrowthOpportunities(laptop,{raw:"laptop",category:"laptop",maxBudget:56000,requirements:{}});assert.ok(r.every(x=>x.relatedProducts.every(p=>p.price+laptop.price<=56000)));assert.equal(generateGrowthOpportunities(laptop,{raw:"laptop",category:"laptop",maxBudget:38000,requirements:{}}).length,0)});
