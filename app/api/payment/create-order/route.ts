import { NextResponse } from "next/server"; import { createTrustedOrder } from "../../../../lib/payments/service";
export async function POST(request:Request){try{const body=await request.json() as {transactionId?:unknown};if(typeof body.transactionId!=="string")throw new Error("PAYMENT_NOT_READY");const order=await createTrustedOrder(body.transactionId);return NextResponse.json(order)}catch(error){const code=error instanceof Error?error.message:"";const message=code==="CREDENTIALS_MISSING"?"Razorpay Test Mode credentials not configured.":"Payment order could not be created.";return NextResponse.json({error:message},{status:400})}}
