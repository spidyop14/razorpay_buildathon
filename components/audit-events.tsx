"use client";
import { usePolicies } from "./policy-provider";
export function AuditEvents(){const {audit,session}=usePolicies();if(!audit.length)return <div className="rounded-xl border border-dashed border-zinc-300 bg-white p-8 text-center"><p className="font-semibold">No transaction events yet</p><p className="mt-2 text-sm text-zinc-500">Start AI Buyer discovery to build the auditable agent workflow.</p></div>;return <div className="overflow-x-auto rounded-xl border border-zinc-200 bg-white"><table className="w-full min-w-180 text-left text-sm"><thead className="border-b border-zinc-100 bg-zinc-50 text-xs uppercase tracking-wider text-zinc-500"><tr>{["Time","Actor","Action","Amount","Policy / context","Result"].map(x=><th key={x} className="px-5 py-3 font-semibold">{x}</th>)}</tr></thead><tbody>{audit.map((r,i)=><tr key={`${r.time}-${i}`} className="border-b border-zinc-100 last:border-0"><td className="px-5 py-4 text-zinc-600">{r.time}</td><td className="px-5 py-4 font-semibold">{r.actor}</td><td className="px-5 py-4 text-zinc-600">{r.action}</td><td className="px-5 py-4 text-zinc-600">{r.amount}</td><td className="px-5 py-4 text-zinc-600">{r.policy}<span className="block pt-1 text-xs text-zinc-400">Session {session.sessionId.slice(-6)}</span></td><td className="px-5 py-4"><span className={r.result==="Blocked"?"font-semibold text-rose-600":r.result==="Pending"?"font-semibold text-amber-600":"font-semibold text-emerald-600"}>{r.result}</span></td></tr>)}</tbody></table></div>}
